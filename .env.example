PORT=8787
HOST=127.0.0.1
# Optional. When omitted, invite links use the current request host.
PUBLIC_BASE_URL=
GROUP_RELAY_DATA_DIR=./data
MAX_FILE_SIZE_MB=25

# Used only by `npm run mcp`
GROUP_RELAY_URL=http://127.0.0.1:8787
GROUP_RELAY_GROUP_ID=
GROUP_RELAY_MEMBER_TOKEN=
